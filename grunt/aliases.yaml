test:
  description: 'Test that code'
  tasks:
    - 'clean:test'
    - 'babel'
    - 'karma'

build:
  tasks:
    - 'clean:dist'
    - 'babel'
    - 'concat'
    - 'uglify'

default: ['test', 'build']
